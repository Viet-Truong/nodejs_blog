<div class="container">
    <div>
        <a href="/me/stored/courses">Khoá học của tôi</a>
        <h3>Khoá học đã xoá</h3>
    </div>
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Tên khoá học</th>
                <th scope="col">Mô tả</th>
                <th scope="col" colspan="2">Ảnh</th>
            </tr>
        </thead>
        <tbody>
            {{#each courses}}
                <tr>
                    <th scope="row">{{sum @index 1}}</th>
                    <td>{{this.name}}</td>
                    <td>{{this.description}}</td>
                    <td>
                        <img src="{{this.image}}" width="100px" height="60px"/>
                    </td>
                    <td>
                        <a class="btn btn-link btn-restore" data-id="{{this._id}}">Khôi phục</a>
                        <a href="" class="btn btn-link" data-bs-toggle="modal" data-bs-target="#delete_course" data-id="{{this._id}}" id="btn_delete">Xoá vĩnh viễn</a>
                    </td>
                </tr>

            {{else}}

            <tr>
                <td colspan="5">Thùng rác trống</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</div>


{{!-- Show modal delete course --}}
<div class="modal fade" id="delete_course" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">Xoá</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
            Bạn có chắc chắn muốn xoá khoá học này không?
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-danger" id="btn_delete_of_modal">Xác nhận</button>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Huỷ</button>
        </div>
        </div>
    </div>
</div>

{{!-- DELETE FORM HIDDEN --}}

<form name='form_delete' method="POST"></form>
<form name='form_restore' method="POST"></form>

<script>
    const formDelete = document.forms['form_delete'];
    const formRestore= document.forms['form_restore'];
    const btnDeleteOfModal = document.getElementById('btn_delete_of_modal')
    const btnDelete = document.querySelectorAll('#btn_delete')
    const btnRestore = document.querySelectorAll('.btn-restore')

    let courseID;
    for(var i = 0; i < btnDelete.length; i++) {
        btnDelete[i].onclick = function(e) {
            courseID = this.getAttribute('data-id');
            //console.log(courseID);

            btnDeleteOfModal.onclick= function(e){  // sau đó click vào button confirm xóa để 
                formDelete.action = `/courses/${courseID}/force?_method=DELETE`; // truyền  url chứa id của từng khóa học vào action và với method DELETE
                formDelete.submit();
            }
        }
    }


    for(var i = 0; i < btnRestore.length; i++) {
        btnRestore[i].onclick = function(e) {
            courseID = this.getAttribute('data-id');
            //console.log(courseID);

            formRestore.action = `/courses/${courseID}/restore?_method=PATCH`; // truyền  url chứa id của từng khóa học vào action và với method DELETE
            formRestore.submit();
        }
    }


</script>